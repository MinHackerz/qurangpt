[build]
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "app/api"

# Note: Scheduled functions need to be configured in Netlify dashboard
# The scheduled-cleanup.js function is in netlify/functions/ directory
# To enable scheduling, go to Netlify dashboard > Functions > Scheduled Functions
